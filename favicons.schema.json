{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://tmphat.dev/favicons.schema.json",
  "title": "Favicons",
  "description": "Favicons configuration",
  "type": "object",
  "properties": {
    "baseImage": {
      "type": "string",
      "description": "Path to the image used as a base for favicon generation."
    },
    "appName": {
      "type": "string",
      "description": "Data used to generate webmanifest file describing the website."
    },
    "appShortName": {
      "type": "string",
      "description": "Data used to generate webmanifest file describing the website."
    },
    "appDescription": {
      "type": "string",
      "description": "Data used to generate webmanifest file describing the website."
    },
    "lang": {
      "type": "string",
      "description": "Data used to generate webmanifest file describing the website."
    }
  },
  "required": ["baseImage", "appName", "lang"]
}
