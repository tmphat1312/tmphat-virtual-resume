---
import type { HTMLAttributes } from "astro/types";
import { cn } from "../utils/class-name";

type Props = HTMLAttributes<"p">;

const { class: className, ...props } = Astro.props;
---

<p class={cn("text-fg/80 dark:text-bg/80 my-6", className)} {...props}>
  <slot />
</p>
